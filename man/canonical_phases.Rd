% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-vocab.R
\name{canonical_phases}
\alias{canonical_phases}
\title{Canonical phase vocabulary}
\usage{
canonical_phases()
}
\value{
A character vector of the canonical phase names.
}
\description{
Four lowercase words: \code{rest}, \code{warmup}, \code{exercise}, \code{recovery}.
}
\details{
This set is not arbitrary. It is the largest set that every existing
downstream matcher already accepts, so normalising to it needs no change
anywhere else in the package. In particular \code{cooldown} is deliberately NOT
produced: the exclusion list in \code{filter_exercise_data()} holds the literal
\code{"cool"} and tests with \code{\%in\%}, not \code{grepl}, so a \code{cooldown} value would
slip through and contaminate the exercise window of every plot. Cooldown
folds into \code{recovery}, which every site accepts.
}
\keyword{internal}
