% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-thresholds.R
\name{detect_threshold_range}
\alias{detect_threshold_range}
\title{Detect VT1 and VT2 as Ranges Across Methods and Smoothing}
\usage{
detect_threshold_range(
  breath_df,
  methods = NULL,
  smoothing = c("default", "narrow", "wide"),
  stages = NULL
)
}
\arguments{
\item{breath_df}{A breath-by-breath tibble with columns \code{time_s},
\code{vo2_ml}, \code{vco2_ml}, \code{ve_l}, optionally \code{peto2_mmhg}, \code{petco2_mmhg}.}

\item{methods}{Character vector restricting methods to try. If \code{NULL},
all supported methods are used: \code{"v_slope"}, \code{"ve_vo2"}, \code{"peto2"}
for VT1; \code{"ve_vco2"}, \code{"petco2"} for VT2.}

\item{smoothing}{Character vector of smoothing presets to vary:
\code{"narrow"} (~10 s), \code{"default"} (~30 s), \code{"wide"} (~60 s) rolling
windows on VO2/VCO2/VE.}

\item{stages}{Optional stages tibble with \code{time_s} and \code{stage}. When
supplied, leading seated-rest breaths (stage 0) are excluded before
detection so resting values don't shift VT1/VT2 downward.}
}
\value{
A list with elements:
\describe{
\item{vt1_range}{\code{c(low, high)} VO2 (mL/min) across successful
VT1 combinations, or \code{NULL} if none succeeded.}
\item{vt2_range}{\code{c(low, high)} VO2 (mL/min) for VT2, or \code{NULL}.}
\item{vt1_values}{Tibble of \code{method}, \code{smoothing}, \code{vo2} for
every successful VT1 combination.}
\item{vt2_values}{Tibble for VT2.}
}
}
\description{
Runs the existing threshold detectors for every combination of method
and smoothing preset and returns the min/max VO2 across all successful
combinations as a range. This surfaces detection uncertainty driven by
protocol-dependent choices (Jamnick et al. 2020).
}
