% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resting.R
\name{compute_resting_values}
\alias{compute_resting_values}
\title{Compute Resting Values from the Leading Rest Block}
\usage{
compute_resting_values(data_avg, stages = NULL, window_s = 60)
}
\arguments{
\item{data_avg}{A \code{CpetData} object (typically after \code{average()}), carrying
\verb{@breaths}, \verb{@stages}, \verb{@participant}, and \verb{@averaging_window}.}

\item{stages}{Optional stages tibble with \code{time_s} and \code{stage} columns. When
\code{NULL} (default), \code{data_avg@stages} is used.}

\item{window_s}{Target averaging window at the end of the rest block, in
seconds. Capped to the actual rest-block duration. Default: 60.}
}
\value{
A named list with \code{duration_s}, \code{window_s}, \code{n_breaths},
\code{vo2_rest}, \code{vo2_kg_rest}, \code{vco2_rest}, \code{ve_rest}, \code{hr_rest},
\code{rer_rest}, and \code{averaging_s}. Returns \code{NULL} when no usable rest block
is detected.
}
\description{
Summarises the last \code{window_s} seconds of the leading seated-rest period
(breaths tagged \code{stage == 0} by \code{\link[=extract_stages]{extract_stages()}}) into descriptive means
for VO2, VCO2, VE, HR and RER. These are \strong{not} a formal basal metabolic
rate: the subject is on the ergometer and not in a fasted supine state.
Returns \code{NULL} when no leading rest block is present or when it is shorter
than 20 s.
}
\examples{
\dontrun{
  analysis@resting <- compute_resting_values(analysis@data)
}
}
