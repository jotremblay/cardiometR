% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-columns.R
\name{resolve_columns}
\alias{resolve_columns}
\title{Resolve source column names to canonical names}
\usage{
resolve_columns(
  raw_names,
  lookup,
  ignore = character(),
  ignore_patterns = character(),
  user_mapping = NULL
)
}
\arguments{
\item{raw_names}{Character vector of source column headers.}

\item{lookup}{Named character vector mapping a normalised key to a canonical
column name.}

\item{ignore}{Character vector of source headers that are known to this
format but unused, so they can be dropped without a warning.}

\item{ignore_patterns}{Character vector of regular expressions, matched
against the folded header, for whole families of unused columns such as
the 24 ECG channels.}

\item{user_mapping}{Optional named character vector, \code{c("Raw Header" = "canonical")}, which overrides \code{lookup}.}
}
\value{
A list with:
\describe{
\item{mapping}{Named character vector, source name to canonical name,
\code{NA} where unmatched.}
\item{unknown}{Source names that matched nothing and are not ignored.}
\item{ignored}{Source names dropped by the ignore list.}
\item{conflicts}{Data frame of canonical names claimed more than once.}
\item{suggestions}{Named list of near-miss hints for \code{unknown}.}
\item{inline_units}{Named character vector of units taken from trailing
parentheticals.}
}
}
\description{
Matching is by exact alias only, after folding with \code{\link[=norm_key]{norm_key()}}. Fuzzy
matching is deliberately not used: the canonical CPET names sit one edit
apart from each other (\code{vo2}/\code{vco2}, \code{ve}/\code{vt}, \code{rq}/\code{rf}, \code{peo2}/\code{feo2}), so
any edit-distance matcher can map VCO2 onto VO2 and produce an RER of 1.0
everywhere with no error at all. Near misses become suggestions for the user
to act on, never silent substitutions.
}
\keyword{internal}
