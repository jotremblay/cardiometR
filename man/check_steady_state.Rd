% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steady-state.R
\name{check_steady_state}
\alias{check_steady_state}
\title{Per-Stage Steady-State Check}
\usage{
check_steady_state(breath_df, stage_summary)
}
\arguments{
\item{breath_df}{Breath-by-breath (or averaged) tibble with \code{time_s},
\code{stage}, \code{hr_bpm} (optional), and \code{rer} columns.}

\item{stage_summary}{Stage summary tibble keyed on \code{stage} (used to
align with the caller's stage identifiers).}
}
\value{
A tibble keyed on \code{stage} with columns \code{duration_ok},
\code{hr_drift_bpm}, \code{rer_drift}, \code{rer_sub_unity}, \code{steady_state_ok}.
}
\description{
For each stage in a CPET, evaluate whether the stage satisfies the
steady-state assumption required for indirect-calorimetry substrate
oxidation: stage duration >= 3 min, HR drift < 3 bpm, RER drift < 0.03
across the last 90 s, and sub-unity RER (max RER < 1.0 in the last 90 s).

HR drift is computed as \verb{mean(HR[last 30s]) - mean(HR[prior 60s])}
within each stage; RER drift is the same for RER.
}
\references{
Achten J, Jeukendrup AE. 2004. Optimizing fat oxidation
through exercise and diet. Nutrition 20:716-727.
}
