% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-report.R
\name{preview_cpet_columns}
\alias{preview_cpet_columns}
\title{See how a data file's columns will be interpreted}
\usage{
preview_cpet_columns(
  file,
  format = NULL,
  sheet = NULL,
  quiet = !rlang::is_interactive()
)
}
\arguments{
\item{file}{Path to the data file.}

\item{format}{Format name, such as \code{"cosmed"}. \code{NULL} detects it from the
file. See \code{\link[=list_cpet_dialects]{list_cpet_dialects()}}.}

\item{sheet}{Sheet name or number, for spreadsheets. \code{NULL} uses the sheet
the format expects, falling back to the first one.}

\item{quiet}{Suppress the summary printed to the console. Defaults to \code{TRUE}
outside an interactive session.}
}
\value{
A data frame with one row per column found in the file:
\describe{
\item{source}{The header as written in the file.}
\item{canonical}{The internal name it maps to, or \code{NA}.}
\item{unit_file}{The unit the file states, if any.}
\item{unit_target}{The unit cardiometR will convert it to.}
\item{status}{\code{"mapped"}, \code{"ignored"} for columns this format is known
to write but cardiometR does not use, or \code{"unrecognised"}.}
\item{suggestion}{A close alias, when there is one.}
}
The detected format and layout are attached as attributes.
}
\description{
Reads only the top of a file, works out which format it is and where its
column headers are, and reports what each column would become. Nothing is
imported and no data is read, so this works even on a file that
\code{\link[=read_cpet]{read_cpet()}} refuses.
}
\details{
Use it when a file half-imports, or when a column you expected is missing.
The \code{source} column gives the header exactly as the file spells it, which is
what you pass to \code{read_cpet(mapping = ...)}.
}
\examples{
file <- system.file("extdata", "example_cosmed.xlsx", package = "cardiometR")
preview <- preview_cpet_columns(file, quiet = TRUE)
head(preview)

}
\seealso{
\code{\link[=read_cpet]{read_cpet()}}, \code{\link[=list_cpet_dialects]{list_cpet_dialects()}}
}
