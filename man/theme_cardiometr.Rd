% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_cardiometr}
\alias{theme_cardiometr}
\title{cardiometR ggplot2 Theme}
\usage{
theme_cardiometr(base_size = 12, dark = FALSE)
}
\arguments{
\item{base_size}{Base font size (default 12).}

\item{dark}{If \code{TRUE}, emit a dark-mode variant (transparent panel
backgrounds, light axis/text colours) so ggplot output reads on a
dark Shiny shell. Default \code{FALSE} is identical to the original
light-on-white theme used by Typst PDF reports.}
}
\value{
A ggplot2 theme object.
}
\description{
A minimalist ggplot2 theme built on \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}} tuned for
cardiometR reports and the Shiny app. Uses an Inter/sans font stack,
strong axis titles, subtle panel grid, white background, and 0.8 lwd
axis lines. Designed to read well under light and dark UI surfaces.
}
\examples{
\dontrun{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) + geom_point() + theme_cardiometr()
}
}
