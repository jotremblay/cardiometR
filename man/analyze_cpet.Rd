% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{analyze_cpet}
\alias{analyze_cpet}
\title{Run the standard CPET analysis pipeline}
\usage{
analyze_cpet(
  data,
  settings = list(),
  participant = NULL,
  threshold_override = NULL
)
}
\arguments{
\item{data}{A \code{CpetData} object from \code{read_cpet()} / \code{read_cosmed()}.}

\item{settings}{Optional named list of analysis settings. Recognised keys:
\code{averaging_method}, \code{averaging_window}, \code{threshold_methods}, \code{protocol},
\code{stage_duration}, \code{increment_size}, \code{modality}, \code{starting_intensity},
\code{athlete_sport}, \code{athlete_level}. Missing keys use package defaults.}

\item{participant}{Optional \code{Participant} object that overrides the one
stored on \code{data}.}

\item{threshold_override}{Optional list with \code{vt1_vo2} and/or \code{vt2_vo2}
for a manual threshold correction.}
}
\value{
A \code{CpetAnalysis} object.
}
\description{
Orchestrates averaging, peak detection, ventilatory thresholds, stage
extraction, and Phase-1 metrics into a \code{CpetAnalysis} object. This is the
programmatic counterpart of the Shiny Results tab pipeline.
}
\examples{
\dontrun{
data <- read_cpet("path/to/export.xlsx")
analysis <- analyze_cpet(data)
plot_cpet_panel(analysis)
generate_report(analysis, "report.pdf", ReportConfig())
}

}
