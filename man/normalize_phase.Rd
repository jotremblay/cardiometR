% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-vocab.R
\name{normalize_phase}
\alias{normalize_phase}
\title{Map free-text phase labels to the canonical vocabulary}
\usage{
normalize_phase(x, vocab = NULL)
}
\arguments{
\item{x}{Character vector of phase labels as written in the file.}

\item{vocab}{Optional named character vector, normalised key to canonical
phase, merged over the built-in vocabulary.}
}
\value{
A list with \code{values} (the canonical vector), \code{table} (a data frame
of raw label, canonical name and row count) and \code{unmapped} (the distinct
labels that matched nothing).
}
\description{
Unrecognised labels become \code{NA}, never a guess. Every downstream site either
guards with \code{!is.na(phase)} or tolerates \code{NA}, so the pipeline falls back to
power-based stage detection, which is the right conservative answer.
}
\keyword{internal}
